# schema.graphqls

#import "schemaFactura.graphqls"
#import "schemaUsuario.graphqls"

input newFactura{
    id_usuario: String! # Corregido: asegúrate de que Usuario esté definido en el mismo archivo o en un archivo importado
    total: Float!
    fecha: String!
}

type Query {
  facturas: [Factura]
  factura(nro: ID!): Factura
  usuarios: [Usuario]
  usuario(id: ID!): Usuario
}

type Mutation {
  createFactura(id_usuario: String!, total: Float!, fecha: String!): Factura
  createUsuario(nombreUsuario: String!, password: String!, admin: Boolean!): Usuario
}
type Factura {
    nro: ID!
    id_usuario: Usuario! # Corregido: asegúrate de que Usuario esté definido en el mismo archivo o en un archivo importado
    total: Float!
    fecha: String!
}

type Usuario {
    id: ID!
    nombre_usuario: String!
    password: String!
    admin: Boolean!
}